apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: app-of-apps
spec:
  destination:
    name: https://kubernetes.default.svc
    namespace: app-of-apps
  source:
    path: apps
    repoURL: 'git@github.com:rockchico/argo-cd-app-of-apps-demo.git'
    targetRevision: HEAD
  project: default
  # syncPolicy:
  #   automated:
  #     prune: true
  #     selfHeal: true